{
  "name": "projeto_cecan",
  "version": "1.0.0",
  "description": "projeto feito para ong CECAN",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "set NODE_ENV=DEV && nodemon -L index",
    "migrate": "cd src/outbound/database/orm/sequelize && cross-env NODE_ENV=DEV npx sequelize-cli db:migrate --migrations-path migration",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.5",
    "pg-hstore": "^2.3.4",
    "uuid": "^9.0.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
